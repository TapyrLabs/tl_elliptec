# protocol

Wire-level encoding: hex-ASCII ⇄ `word`/`short`/`dword`/`long`/`float`, and
message framing. Used internally by {doc}`bus`; most users won't need to
call these directly.

```{automodule} tl_elliptec.protocol
:members:
:undoc-members:
```
